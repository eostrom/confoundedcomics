= page_form_for(@page) do |form|
  = form.input :comic, :required => true
  = form.input :published_at, :label => 'Publish date', :as => :string
  = form.input :title
  = form.input :commentary
  = form.buttons do
    = form.commit_button
